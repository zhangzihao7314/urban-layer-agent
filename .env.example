# ============================================
# GIS-RAG Environment Configuration (Example)
# ============================================
#
# Copy this file to ".env" and adjust values for your machine.
# Do not commit your real ".env" to GitHub.
#

# ==================== Core Model Settings ====================
# Instruct model (required for generation; leave empty to run retrieval-only)
LLM_INSTRUCT_MODEL_PATH=
LLM_INSTRUCT_MODEL_NAME=Qwen/Qwen3-4B-Instruct-2507-FP8 #suitable for 8GB VRAM

# Thinking model (optional; used when enabling Deep Think)
LLM_THINKING_MODEL_PATH=
LLM_THINKING_MODEL_NAME=Qwen/Qwen3-4B-Thinking-2507-FP8  #suitable for 8GB VRAM

# Device: "cuda" or "cpu"
DEVICE=cuda

# ==================== Embedding Settings ====================
# Embedding model name on HuggingFace (or local path if EMBEDDING_MODEL_PATH is set)
EMBEDDING_MODEL_NAME=BAAI/bge-m3

# Optional: local embedding model directory (leave empty to download automatically)
EMBEDDING_MODEL_PATH=

# Max input length for local embedding encoder (only used by some local embedding paths)
EMBEDDING_MAX_LENGTH=256

# Retrieval mode: "dense" or "hybrid"
RETRIEVAL_MODE=dense
HYBRID_DENSE_WEIGHT=0.7
HYBRID_LEXICAL_WEIGHT=0.3
HYBRID_CANDIDATE_MULTIPLIER=5

# ==================== App Basics ====================
PROJECT_NAME=GIS-RAG
VERSION=1.0.0
DEBUG=False

# ==================== Service Settings ====================
API_HOST=127.0.0.1
API_PORT=8000
WEB_HOST=127.0.0.1
WEB_PORT=8501

# ==================== Database Settings ====================
VECTOR_DB_TYPE=chroma

# ==================== File Handling ====================
# Max file size (bytes)
MAX_FILE_SIZE=104857600

# ==================== GIS Settings ====================
DEFAULT_CRS=EPSG:4326
MAX_VECTOR_FEATURES=10000

# ==================== Text Processing ====================
CHUNK_SIZE=4000
CHUNK_OVERLAP=500

# ==================== Retrieval Settings ====================
TOP_K=3
SIMILARITY_THRESHOLD=0.5
